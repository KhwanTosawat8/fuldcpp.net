# The updater signature is computed over the exact LF bytes of version.xml.
# Never let git rewrite line endings on these, or signature verification fails.
update/version.xml       -text
update/**/version.xml    -text
*.sign                   binary
*.zip                    binary
